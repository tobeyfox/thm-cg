{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Active Project",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/bin/engine",
            "args": [],
            //"stopAtEntry": false,
            "cwd": "${fileDirname}",
            // "environment": [],
            // "externalConsole": false,
            // "MIMode": "lldb",
            "preLaunchTask": "Make"
        }
    ]
}